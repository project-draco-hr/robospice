{
  super.cancel();
  Log.w(RestContentRequest.class.getName(),"Cancel can't be invoked directly on " + RestContentRequest.class.getName() + " requests. You must call SpiceManager.cancelAllRequests().");
}
