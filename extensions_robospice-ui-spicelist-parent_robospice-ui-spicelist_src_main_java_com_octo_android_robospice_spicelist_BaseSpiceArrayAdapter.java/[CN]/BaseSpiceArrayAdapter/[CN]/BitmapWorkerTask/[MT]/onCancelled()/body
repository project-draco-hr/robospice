{
  if (animationIn != null) {
    animationIn.cancel();
  }
  if (animationOut != null) {
    animationOut.cancel();
  }
  ImageView imageView=imageViewReference.get();
  if (imageView != null) {
    imageView.setHasTransientState(false);
  }
}
