{
  lock.lock();
  try {
    checkIsExectuedInUIThread();
    isSuccessful=false;
    this.exception=exception;
    if (isComplete) {
      requestFinishedCondition.signal();
    }
  }
  finally {
    lock.unlock();
  }
}
